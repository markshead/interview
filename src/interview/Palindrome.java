package interview;

public class Palindrome {

	/**
	 * Modify this method so it returns true if input is a palindrome
	 * and false if it is not. A palindrome string is one that reads
	 * the same backwards and forwards.
	 */
	public boolean isPalindrome(String input) {
		return true;
	}
}
